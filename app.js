// Global App Namespace & Initialization
window.App = {
    init() {
        console.log("QIFY Photo Print Studio Initialized");
    }
};

document.addEventListener('DOMContentLoaded', () => {
    App.init();
});